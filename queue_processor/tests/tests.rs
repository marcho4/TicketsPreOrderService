#[cfg(test)]
mod tests {
    use queue_processor::QueueService;
    use queue_processor::utils::config::Config;

    #[test]
    fn test_add() {
        // Тест будет выполняться после создания lib.rs и добавления импортов
        // QueueService теперь должен быть виден в тестах
        // Примечание: для реальных тестов нужно будет использовать асинхронные тесты
        // и настроить моки для зависимостей
    }
}